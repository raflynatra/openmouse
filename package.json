{
  "name": "openmouse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "node --test \"src/**/*.test.ts\"",
    "size": "node build/check-bundle-size.ts",
    "check": "npm run build && npm test",
    "preview": "vite preview"
  },
  "dependencies": {
    "@openmouse/protocol": "https://github.com/raflynatra/mouse-protocol.git#fantech-helios-ii-xd3-v3"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
